name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: echo ${{ github.event.unknown_property }}
      - name: Bad expression
        if: ${{ github.ref = 'refs/heads/main' }}
